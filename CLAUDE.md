# CLAUDE.md

이 파일은 Claude Code가 이 프로젝트에서 세션 시작 시 자동으로 읽는 프로젝트 지침입니다.

## Git push 전 필수 절차

`git push`를 하기 전에는 항상 다음 순서를 따릅니다.

1. `CHANGELOG.md`에 기록을 남긴다.
   - 오늘 날짜의 `## YYYY-MM-DD` 섹션이 없으면 새로 만든다.
   - 같은 날짜 섹션 안에서는 커밋 순서대로 번호(`### 1.`, `### 2.`, ...)를 이어 붙인다. 번호는 그날 마지막 항목 다음 번호부터 시작한다.
   - 각 항목에는 커밋 해시(또는 "예정"), 커밋 메시지 요약, 무엇을·왜 바꿨는지 짧은 불릿으로 정리한다.
   - 변경 배경이 길면 별도 md 파일(예: `JWT_SECURITY_CHANGES.md`처럼 주제가 드러나는 이름)로 분리하고 `CHANGELOG.md`에서 링크만 남긴다.
2. 그 다음 커밋을 만들고 push한다.

이 절차는 사용자가 매번 요청하지 않아도 push할 때마다 자동으로 수행합니다.
